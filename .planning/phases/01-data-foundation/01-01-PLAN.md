---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/db.js
  - scripts/lib/config.js
  - scripts/init-db.js
  - scripts/tests/test-db.js
  - .env.example
  - .gitignore
  - package.json
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
user_setup:
  - service: epc-register
    why: "EPC data extraction for D-G rated rental properties"
    env_vars:
      - name: EPC_EMAIL
        source: "Email used to register at https://epc.opendatacommunities.org/"
      - name: EPC_API_KEY
        source: "API key from EPC Register account"
  - service: companies-house
    why: "SPV landlord identification and entity classification"
    env_vars:
      - name: COMPANIES_HOUSE_API_KEY
        source: "API key from https://developer.company-information.service.gov.uk/"

must_haves:
  truths:
    - "SQLite database can be created from scratch by running init-db.js"
    - "landlords table stores entity type (ltd/llp/individual/unknown), company number, source provenance, and scraped timestamp"
    - "properties table stores UPRN, address, postcode, EPC rating, tenure, lodgement date, certificate number, and links to landlord via landlord_id"
    - "Database tests pass confirming schema creation, inserts, and queries"
    - "MCP servers (Apify, Playwright, Companies House) are configured in Claude Code"
  artifacts:
    - path: "scripts/init-db.js"
      provides: "Database schema creation script"
      contains: "CREATE TABLE IF NOT EXISTS landlords"
    - path: "scripts/lib/db.js"
      provides: "Shared database connection with WAL mode"
      exports: ["db"]
    - path: "scripts/lib/config.js"
      provides: "Shared configuration (postcodes, API URLs, SIC codes)"
      exports: ["POSTCODES", "PROPERTY_SIC_CODES"]
    - path: "scripts/tests/test-db.js"
      provides: "Database unit tests"
      contains: "node:test"
    - path: ".env.example"
      provides: "Template for required environment variables"
      contains: "EPC_API_KEY"
  key_links:
    - from: "scripts/init-db.js"
      to: "scripts/lib/db.js"
      via: "require('./lib/db')"
      pattern: "require.*lib/db"
    - from: "scripts/lib/db.js"
      to: "data/sjb-leads.db"
      via: "Database constructor path"
      pattern: "sjb-leads\\.db"
---

<objective>
Create the project scaffold, SQLite database schema, shared libraries, and MCP server configuration that all subsequent Phase 1 scripts depend on.

Purpose: Every scraper in this phase needs a database to write to, shared config for postcodes/API URLs, and a database connection helper. This plan establishes those foundations plus the test infrastructure.

Output: Working database init script, shared db/config modules, test scaffold, MCP server configuration, .env template.
</objective>

<execution_context>
@C:/Users/sam_1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sam_1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffold, shared libraries, and database schema</name>
  <files>scripts/lib/db.js, scripts/lib/config.js, scripts/init-db.js, scripts/tests/test-db.js, .env.example, .gitignore, package.json</files>
  <read_first>
    - package.json (current dependencies — only has @playwright/test)
    - .gitignore (current contents to append to, not overwrite)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (database schema, code examples, architecture patterns)
  </read_first>
  <behavior>
    - Test 1: Running init-db.js creates `data/sjb-leads.db` with `landlords` table containing columns: id, name, entity_type, company_number, registered_address, sic_codes, source, source_ref, scraped_at, created_at
    - Test 2: Running init-db.js creates `properties` table containing columns: id, landlord_id, uprn, address, postcode, current_energy_rating, property_type, tenure, transaction_type, lodgement_date, certificate_number, source, source_ref, scraped_at, created_at
    - Test 3: entity_type CHECK constraint rejects values outside ('ltd', 'llp', 'individual', 'unknown')
    - Test 4: properties.uprn UNIQUE constraint prevents duplicate UPRN inserts
    - Test 5: properties.landlord_id foreign key references landlords(id)
    - Test 6: Indexes exist on properties(postcode), properties(current_energy_rating), properties(landlord_id), landlords(entity_type), landlords(company_number)
    - Test 7: db.js exports a Database instance with WAL journal mode and foreign keys enabled
    - Test 8: config.js exports POSTCODES array containing SK1-SK8, M14, M19, M20, M21, M22
  </behavior>
  <action>
    1. Install dependencies: `npm install better-sqlite3 dotenv`

    2. Create `scripts/lib/config.js`:
       - Export `POSTCODES` array: `['SK1','SK2','SK3','SK4','SK5','SK6','SK7','SK8','M14','M19','M20','M21','M22']`
       - Export `EPC_API_BASE` = `'https://epc.opendatacommunities.org/api/v1/domestic/search'`
       - Export `CH_API_BASE` = `'https://api.company-information.service.gov.uk'`
       - Export `PROPERTY_SIC_CODES` = `'68100,68209,68320'`
       - Export `DB_PATH` = `path.join(__dirname, '..', '..', 'data', 'sjb-leads.db')`

    3. Create `scripts/lib/db.js`:
       - `require('dotenv').config()` at top
       - `const Database = require('better-sqlite3')`
       - Ensure `data/` directory exists using `fs.mkdirSync` with `{ recursive: true }`
       - Open database at DB_PATH from config.js
       - Set `pragma('journal_mode = WAL')` and `pragma('foreign_keys = ON')`
       - Export the db instance: `module.exports = db`

    4. Create `scripts/init-db.js`:
       - Require db from `./lib/db`
       - Run the exact schema SQL from RESEARCH.md (the CREATE TABLE and CREATE INDEX statements in the "Database Schema (Recommended)" section)
       - `landlords` table: id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, entity_type TEXT CHECK(entity_type IN ('ltd','llp','individual','unknown')) DEFAULT 'unknown', company_number TEXT, registered_address TEXT, sic_codes TEXT, source TEXT NOT NULL, source_ref TEXT, scraped_at TEXT NOT NULL, created_at TEXT DEFAULT (datetime('now')), UNIQUE(company_number)
       - `properties` table: id INTEGER PRIMARY KEY AUTOINCREMENT, landlord_id INTEGER REFERENCES landlords(id), uprn TEXT, address TEXT NOT NULL, postcode TEXT NOT NULL, current_energy_rating TEXT CHECK(current_energy_rating IN ('A','B','C','D','E','F','G')), property_type TEXT, tenure TEXT, transaction_type TEXT, lodgement_date TEXT, certificate_number TEXT, source TEXT NOT NULL, source_ref TEXT, scraped_at TEXT NOT NULL, created_at TEXT DEFAULT (datetime('now')), UNIQUE(uprn), UNIQUE(certificate_number)
       - Indexes: idx_properties_postcode ON properties(postcode), idx_properties_rating ON properties(current_energy_rating), idx_properties_landlord ON properties(landlord_id), idx_landlords_entity_type ON landlords(entity_type), idx_landlords_company_number ON landlords(company_number)
       - Print confirmation: `console.log('Database initialised at data/sjb-leads.db')`
       - Use `db.exec()` for DDL statements (CREATE TABLE, CREATE INDEX)

    5. Create `scripts/tests/test-db.js`:
       - Use `const { describe, it, before, after } = require('node:test')` and `const assert = require('node:assert')`
       - Use a TEMPORARY test database (not the real data/sjb-leads.db) — create in `os.tmpdir()` or `data/test-sjb-leads.db`, clean up after
       - Write tests for all 8 behaviors listed above
       - For schema introspection use `PRAGMA table_info(tablename)` and `PRAGMA index_list(tablename)`

    6. Create `.env.example` with these exact lines:
       ```
       EPC_EMAIL=your-email@example.com
       EPC_API_KEY=your-epc-api-key
       COMPANIES_HOUSE_API_KEY=your-companies-house-api-key
       ```

    7. Update `.gitignore` — append these lines if not already present:
       ```
       data/
       .env
       node_modules/
       ```

    8. Update `package.json` — add `better-sqlite3` and `dotenv` to dependencies (keep existing @playwright/test)

    IMPORTANT: Plain JavaScript only (CommonJS require, no TypeScript, no ES modules). All scripts use `require()` not `import`.
    IMPORTANT: db.js must ensure the `data/` directory exists before opening the database.
    IMPORTANT: Tests must use a separate temporary database, NOT the production data/sjb-leads.db.
  </action>
  <verify>
    <automated>node scripts/init-db.js && node --test scripts/tests/test-db.js</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/lib/db.js` contains `pragma('journal_mode = WAL')` and `pragma('foreign_keys = ON')`
    - `scripts/lib/db.js` contains `sjb-leads.db`
    - `scripts/lib/config.js` contains `POSTCODES` and `SK1` and `M14` and `68209`
    - `scripts/init-db.js` contains `CREATE TABLE IF NOT EXISTS landlords` and `CREATE TABLE IF NOT EXISTS properties`
    - `scripts/init-db.js` contains `entity_type IN ('ltd','llp','individual','unknown')`
    - `scripts/init-db.js` contains `CREATE INDEX idx_properties_postcode`
    - `.env.example` contains `EPC_EMAIL=` and `EPC_API_KEY=` and `COMPANIES_HOUSE_API_KEY=`
    - `.gitignore` contains `data/` and `.env`
    - `package.json` contains `better-sqlite3` and `dotenv`
    - `node --test scripts/tests/test-db.js` exits 0
    - `node scripts/init-db.js` creates `data/sjb-leads.db` without errors
  </acceptance_criteria>
  <done>Database schema creates successfully with all tables, constraints, and indexes. Tests pass confirming schema correctness. Shared db.js and config.js modules export expected values. .env.example documents all required API keys.</done>
</task>

<task type="auto">
  <name>Task 2: MCP server configuration</name>
  <files>.claude/settings.json</files>
  <read_first>
    - .claude/settings.json (if exists — current MCP config)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (MCP server details — companies-house-mcp-server package)
  </read_first>
  <action>
    Configure MCP servers for Claude Code orchestration. Three servers needed:

    1. **Apify MCP** — Should already be connected (per RESEARCH.md "Apify MCP already connected"). Verify it exists in config. If not, add it.

    2. **Playwright** — Already installed as a skill (playwright-cli). No additional MCP config needed. Just verify it works.

    3. **Companies House MCP** — Install and configure:
       - Install: `npm install -g companies-house-mcp-server` (or add to project devDependencies)
       - The MCP server needs `COMPANIES_HOUSE_API_KEY` env var
       - Add to Claude Code MCP config (`.claude/settings.json` or via `claude mcp add`)

    Use `claude mcp add` CLI command to add the Companies House MCP server if not already configured. The command format is:
    ```
    claude mcp add companies-house -- npx companies-house-mcp-server
    ```

    Verify all three are responding:
    - Check MCP server list includes Apify, Playwright, Companies House

    NOTE: If `claude mcp add` is not available or the companies-house-mcp-server package doesn't work as expected, document the issue and proceed — the standalone `scripts/companies-house.js` script (Plan 03) will handle CH queries directly via HTTP. The MCP server is a convenience for interactive Claude Code use, not a hard dependency.
  </action>
  <verify>
    <automated>node -e "try { require.resolve('companies-house-mcp-server'); console.log('CH MCP package found') } catch(e) { console.log('CH MCP not installed as dependency - OK if using global or claude mcp add') }" && echo "MCP config task complete"</automated>
  </verify>
  <acceptance_criteria>
    - Companies House MCP server is either installed globally or added via claude mcp add
    - Apify MCP is confirmed present in configuration
    - No errors when listing MCP servers
  </acceptance_criteria>
  <done>MCP servers configured — Apify (already connected), Playwright (skill installed), Companies House (MCP server added). Claude Code can orchestrate queries to all three services.</done>
</task>

</tasks>

<verification>
1. `node scripts/init-db.js` runs without errors and creates `data/sjb-leads.db`
2. `node --test scripts/tests/test-db.js` — all tests pass
3. `scripts/lib/db.js` and `scripts/lib/config.js` exist and export expected values
4. `.env.example` documents all 3 required environment variables
5. MCP servers are configured for Claude Code
</verification>

<success_criteria>
- Database can be created from scratch by running `node scripts/init-db.js`
- All database tests pass: schema validation, constraint checks, index verification
- Shared libraries (db.js, config.js) are importable by other scripts
- .env.example documents EPC_EMAIL, EPC_API_KEY, COMPANIES_HOUSE_API_KEY
- data/ and .env are gitignored
- MCP servers configured for Claude Code orchestration
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
